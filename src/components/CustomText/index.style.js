import { Platform, StyleSheet } from 'react-native'

// const IS_ANDROID = Platform.OS === 'android'

export default StyleSheet.create({
  text: {
    // fontFamily: IS_ANDROID ? undefined : 'FiraSans-Regular'
  }
})
